# -*- coding: utf-8 -*-

from setuptools import setup, find_packages

setup(
    name='cpe-machine-setup',
    version='0.0.1',
    description="MuleSoft collaborative programming exercise machine setup",
    license='UNLICENSED',
    packages=find_packages(exclude=('tests'))
)
